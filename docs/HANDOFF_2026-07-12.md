# Conversation Handoff - 2026-07-12

## Start here

The next conversation should read these files before changing code:

1. `AGENTS.md`.
2. `CODEX_HANDOFF.md`.
3. `research/competition_policy.md`.
4. `docs/submission_strategy.md`.
5. This handoff.

Continue directly in `C:\Users\DELL\Documents\medical2-race-viettel`.
The user previously authorized inline execution directly on `master`.
Ponytail full mode is active, so prefer the smallest stdlib implementation that preserves strict validation.

## Current repository state

- Branch: `master`.
- HEAD: `d3ab646ec43aaf027432bff1840e14818faf62d8`.
- Remote: `origin` points to `https://github.com/Lolavine777/medical-nlp-retrieval.git`.
- Remote-tracking state: `origin/master` has not been fetched in this workspace.
- Push state: no project commits were pushed during this conversation.
- Fresh verification: 49 tests passed on 2026-07-12.
- Fresh verification: `python -m compileall -q src tools tests` exited successfully.
- Model budget: `0 / 9B` in `configs/model_budget.json`.
- Submission ledger: `docs/submissions.csv` contains only its header and records no submissions.
- Portal quota: no submission was made by this conversation, but actual remaining quota must be checked in the authenticated competition portal.

The following source artifacts are intentionally untracked and must remain unmodified:

- `input.zip`.
- `CODEX_HANDOFF.md`.
- The saved official competition HTML and its asset directory.

The generated artifact `outputs/NON_SUBMITTABLE-empty-output.zip` is intentionally ignored by Git.
Never upload that artifact.

## Organizer constraints and project policy

- Combined active model parameters must not exceed 9B.
- Keep an explicit parameter report before adding any model.
- Prefer shared compact backbones, rules, lexical retrieval, ontology indices, and deterministic postprocessing.
- The ontology, recognition, span, candidate, and position policies are hidden evaluator variables.
- Do not request further organizer clarification for policies declared part of the challenge.
- Never mutate raw text.
- Every emitted span must satisfy `raw_text[start:end] == text` with end-exclusive offsets.
- Preserve duplicate mentions at distinct positions.
- Never invent ICD or RxNorm identifiers.
- Do not restrict RxNorm retrieval to active concepts.
- Keep active and historical concepts, replacement relationships, term types, branded or generic levels, combinations, components, and candidate cardinality configurable.
- Maintain one configurable pipeline rather than diverging codebases.
- Every leaderboard submission must test a documented hypothesis and record commit, config, checksum, parent, prediction diff, score delta, conclusion, confidence, and generalization class.
- Plan around five guaranteed submissions per day and treat legitimate team-member capacity as optional bonus capacity.

## Verified source facts

- `input.zip` contains exactly 100 UTF-8 documents at `input/1.txt` through `input/100.txt`.
- Its SHA-256 is `46fe4a578b2c4478faa7c570b218218f539c0bbf1ea409168ae67a14ad86ca35`.
- The preserved official example contains 19 entities.
- All 19 reconstructed offsets satisfy exact end-exclusive raw slicing.
- The official example preserves repeated surface mentions.
- Observed drug spans include regimen modifiers and exclude separable indications.
- Observed drug objects contain `text`, `type`, `candidates`, `assertions`, and `position`.
- Observed symptom objects contain `text`, `type`, `assertions`, and `position`.
- Diagnosis and laboratory field policies remain hidden hypotheses.
- Official example RxNorm identifiers appear to include obsolete or currently inconsistent mappings.

## Implemented components

### Evidence and offsets

- `tools/audit_sources.py` verifies source membership, hashes, UTF-8, corpus statistics, and saved HTML evidence.
- `src/medical_race/loader.py` preserves raw text and supplies mapped normalized views.
- `src/medical_race/offsets.py` validates exact entity offsets.
- `tests/fixtures/official_example.json` preserves the official example.

### Sections and extraction

- `src/medical_race/sections/__init__.py` parses observed section headers without modifying offsets.
- `src/medical_race/line_roles.py` assigns exact line roles.
- `src/medical_race/extraction/drugs.py` emits conservative drug candidate spans.
- `src/medical_race/extraction/labs.py` emits laboratory name-result pairs.
- Corpus audit result: 82 drug spans across 35 documents.
- Corpus audit result: 51 laboratory pairs across 18 documents.
- Corpus audit result: zero local offset mismatches.
- These are retrieval candidates, not final evaluator-ready entities.

### Assertions

- `src/medical_race/assertions/__init__.py` implements clause-scoped negation, temporality, experiencer, and conservative section priors.
- Current-illness sections are not historical by default.
- Family reporter language differs from family experiencer language.
- Commas, sentence punctuation, newlines, semicolons, `nhưng`, and `tuy nhiên` stop cue propagation.
- `tools/audit_assertions.py` classified 184 current candidate spans.
- Audit result: 64 `isHistorical` labels and zero offset errors.
- Assertion counts are rule behavior, not measured accuracy.

### Serializer and packaging

- `src/medical_race/output.py` enforces strict type-dependent fields, offsets, assertion labels, candidate-list shape, Unicode, order, and duplicate preservation.
- Invalid or incomplete entities reject the entire document.
- The official 19-entity fixture round-trips and has serialized SHA-256 `7dfe76f9f728ca1646e3f768f72460e18f9f567aec5ab20added626551258f93`.
- `src/medical_race/submission/__init__.py` builds an in-memory deterministic ZIP and revalidates all entries before exclusive filesystem creation.
- ZIP entries are exactly `output/1.json` through `output/100.json` in numeric order.
- ZIP metadata is fixed and compression is `ZIP_STORED`.
- The format-only empty dry-run is 10,706 bytes with SHA-256 `7fad042299b7c15f185ce4d083e4b1a55ecdc600d110f9ed85be382dd506bbd4`.

Checkpoint evidence is recorded in:

- `docs/parser_checkpoint_2026-07-11.md`.
- `docs/assertion_checkpoint_2026-07-11.md`.
- `docs/output_checkpoint_2026-07-11.md`.
- `docs/package_checkpoint_2026-07-12.md`.

## Important limitations

- No ICD or RxNorm ontology snapshot has been ingested.
- No candidate linker exists.
- No symptom or diagnosis extractor exists beyond section and policy groundwork.
- The drug parser can emit generic or noisy retrieval candidates that must be filtered by ontology linking.
- The laboratory lexicon is intentionally incomplete.
- Trend expressions currently select the first numeric laboratory value.
- No provisional local evaluator exists.
- No semantic prediction diff tool exists.
- No leaderboard-ready prediction artifact exists.
- No submission has been made or recorded.

## Next task

Implement the provisional local evaluator as the next isolated checkpoint.

Keep evaluator assumptions explicit and configurable.
Do not call its matching behavior official.
The minimum useful evaluator should include:

1. A word-level edit-distance or WER function.
2. Set Jaccard for assertions and candidates with an explicit empty-set convention.
3. Configurable mention matching policies, starting with exact `(type, position)` and exact `(type, text, position)` alternatives.
4. Unmatched prediction and gold penalties represented explicitly.
5. Per-component scores using the documented weights `0.3`, `0.3`, and `0.4`.
6. Tests showing how duplicate mentions are assigned and how changing one matching policy changes only matching behavior.
7. A checkpoint document that distinguishes mathematical implementation from hidden evaluator assumptions.

After the evaluator, prioritize legally usable versioned ICD and RxNorm ingestion and lexical candidate retrieval.
Do not train a large model before the baseline can produce non-empty validated outputs with ontology-backed candidates and local evaluation reports.

## Useful commands

Run tests:

```powershell
$env:PYTHONPATH='src'
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

Compile Python:

```powershell
.\.venv\Scripts\python.exe -m compileall -q src tools tests
```

Audit sources:

```powershell
$env:PYTHONPATH='src'
.\.venv\Scripts\python.exe tools\audit_sources.py
```

Audit assertions:

```powershell
$env:PYTHONPATH='src'
.\.venv\Scripts\python.exe tools\audit_assertions.py
```

Check repository state:

```powershell
git status --short
git log -10 --oneline
git remote -v
```

## Windows editing note

In this environment, `apply_patch` can add files but has intermittently failed when updating existing files with `CreateProcessWithLogonW failed: 5`.
The safe workaround used in this conversation was to create a complete `.new` file with `apply_patch`, verify both resolved paths remain inside the workspace, and then atomically replace the target with PowerShell `Move-Item -LiteralPath ... -Force` under approved escalation.
Do not use shell redirection, Python, or ad hoc write commands to edit project files.

## Suggested opening prompt for the next conversation

Read `AGENTS.md`, `CODEX_HANDOFF.md`, `research/competition_policy.md`, `docs/submission_strategy.md`, and `docs/HANDOFF_2026-07-12.md` completely.
Verify Git state and run the full test suite before changing code.
Then continue inline on `master` with the provisional local evaluator checkpoint described in the handoff.
Keep Ponytail full mode active, use TDD, keep evaluator policies configurable, and do not treat any local matching policy as official.
